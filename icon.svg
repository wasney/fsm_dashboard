<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- Background: Black rectangle with rounded corners (rx=10) -->
  <rect width="100" height="100" rx="10" ry="10" fill="#000000"/>

  <!-- Text: "FSM DASH" in white, centered -->
  <!-- Adjust font-size and y position as needed for visual centering -->
  <text x="50%" y="55%"
        font-family="Arial, sans-serif"
        font-size="20"
        font-weight="bold"
        fill="#FFFFFF"
        text-anchor="middle"
        dominant-baseline="middle">
    FSM DASH
  </text>
</svg>
